/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuração para comunicação com a API NestJS
  // Em produção, esse valor viria de uma variável de ambiente
};

module.exports = nextConfig;
